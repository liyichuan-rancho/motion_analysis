import numpy as np

a = np.zeros(200)
b = np.zeros(200)

# print((a==b).all())
# if (a==b).all() is True

zeros = np.zeros(200)
zeros2 = np.zeros(200)
a = (zeros == zeros2).all()
b = True
# c = if (1 ==1)
print(a)
if (zeros == zeros2).all()  == True:
    print("1")

print((zeros == zeros2).all())